#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf(" Enter any num ");
    scanf(" %d",&num);
    if(num>0)
          printf(" The Number is Positive");
     else
          printf(" The Number is Negative");

    return 0;
}
