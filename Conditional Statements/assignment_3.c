#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf(" Enter any Number ");
    scanf(" %d",&num);
    if(num>0)
          printf(" %d Number is Positive",&num);
     else
          printf(" The Number is Negative");

    return 0;
}
