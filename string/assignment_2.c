//  C program to separate the individual characters from from a string  //

#include <stdio.h>
#include <stdlib.h>

int main()
{
    char c[15]="w3resource.com";
    int length=strlen(c);
    printf("Length of the string is : %d\n",length);
    return 0;
}
